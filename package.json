{
  "name": "contador-clicks",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Contador de clicks con persistencia en localStorage",
  "main": "src/index.js",
  "scripts": {
    "start": "web-dev-server  --open --mode development",
    "start-test": "NODE_ENV=test node app.js",
    "start-prod": "node app.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cypress:run": "cypress run",
    "test": "jest",
    "lint": "oxlint",
    "test:e2e": "npx cypress run"
  },
  "devDependencies": {
    "cypress": "^14.4.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.0.0-beta.3",
    "vite": "^6.3.5",
    "oxlint": "^x.x.x"
  },

  "jest": {
    "testEnvironment": "jest-environment-jsdom"
  }
}
