{
  "name": "contador-clicks",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Contador de clicks con persistencia en localStorage",
  "main": "src/index.js",
  "scripts": {
    "start": "web-dev-server  --open --mode development",
    "start-test": "npm run dev & cypress open",
    "start-prod": "node app.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "cypress run",
    "test": "jest",
    "lint": "oxlint"
  },
  "devDependencies": {
    "cypress": "^14.4.0",
    "globals": "^16.1.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.0-beta.3",
    "vite": "^6.3.5",
    "oxlint": "0.18.0",
    "@babel/preset-env": "^7.27.2"
  },

 "jest": {
    "transform": {
      "^.+\\.js$": "babel-jest"
    },
    "testEnvironment": "jest-environment-jsdom",
    "moduleFileExtensions": ["js", "jsx"]
  }
}
